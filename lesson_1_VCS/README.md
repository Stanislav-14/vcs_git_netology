Second line

---

Прошу прощенья, скорее всего не так понял пункт задания "В файле README.md опишите своими словами, какие файлы будут проигнорированы в будущем благодаря добавленному .gitignore."

Без проблем:  
буду игнорироваться все файлы и директории в следующей директории .terraform/  
буду игнорироваться все файлы с расширением .tfstate в текущей директории и во всех поддиректориях  
.tfstate. также как и в предыдущем примере, только для этого расширения  
.tfvars также и для этого расширения  
.tfvars.json и для этого  
crash.log все файлы с данным именем в текущей и во всех поддиректориях  
crash.*любое кол-во символов*.log все файлы подходящие под этот шаблон в текущей и поддиректориях  
Тоже самое и здесь _override.tf  _override.tf.json  
override.tf  override.tf.json  .terraformrc  terraform.rc  файлы с этими названиями в текущей директории и во всех поддиректориях

звездочка - ноль или несколько символов
